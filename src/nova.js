// nova.js

// var scrollContainer = document.querySelector('.service-cont');
// var scrollItems = document.querySelectorAll('.service-item');
// var currentIndex = 0;
// var scrollSpeed = 0.1;
// var delay = 5000;

// function autoScroll() {
//   // Calculate the next index
//   var nextIndex = currentIndex + 1;

//   // Check if we have reached the last item
//   if (nextIndex >= scrollItems.length) {
//     nextIndex = 0;
//   }

//   // Get the scroll position of the next item
//   var nextScrollPosition = scrollItems[nextIndex].offsetLeft - scrollContainer.offsetLeft;

//   // Animate the scroll to the next item
//   scrollContainer.scrollTo({
//     left: nextScrollPosition,
//     behavior: 'smooth'
//   });

//   // Update the current index
//   currentIndex = nextIndex;

//   // Call the autoScroll function repeatedly after the specified delay
//   setTimeout(autoScroll, delay);
// }

// // Call the autoScroll function after the specified delay
// setTimeout(autoScroll, delay);

